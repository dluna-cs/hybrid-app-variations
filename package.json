{
  "name": "hybrid-apps-workspaces",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "cli": "node ./scripts/cli.js ${0} ${1} ${2}",
    "hook": "node ./scripts/hooks.js ${0}"
  },
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
